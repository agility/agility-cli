import * as mgmtApi from "@agility/management-sdk"; import _ from "lodash"; import { ReferenceMapper } from "../reference-mapper"; export function prepareModelPayload(sourceModel: mgmtApi.Model, referenceMapper: ReferenceMapper): mgmtApi.Model { const payload = _.cloneDeep(sourceModel); delete payload.id; delete payload.lastModifiedDate; delete payload.lastModifiedAuthorID; delete payload.lastModifiedBy; return payload; }
